
MDBoxLayout:
    orientation:'vertical'
    MDTopAppBar:
        title:'Menu'
        left_action_items:[['menu']]
        right_action_items:[["clock", lambda x: app.show_clock_dialog()], ["calendar", lambda x: app.show_calendar_dialog()],['dots-vertical',lambda x:app.callback_1()]]
        elevation:4

    MDLabel:
        id:my_label
        text:'hello darling'
        halign:'center'

    MDBottomAppBar:
        MDTopAppBar:
            icon:'account'
            type:'bottom'
            mode:'free-end'
            icon_color: 0.3, 1, 0, 1

